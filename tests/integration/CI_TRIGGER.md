# CI Integration Trigger

This file is a trivial change to trigger the integration tests workflow.

If you see this PR, it was created to run `.github/workflows/integration-tests.yml` and validate CI wiring.
